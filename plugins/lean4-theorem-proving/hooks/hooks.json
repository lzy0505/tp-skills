{
  "description": "Bootstrap env vars for Lean 4 tooling so slash-commands can find plugin-bundled scripts.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/bootstrap.sh startup"
          }
        ]
      }
    ]
  }
}
